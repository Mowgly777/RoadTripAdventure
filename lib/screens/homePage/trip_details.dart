
import 'package:flutter/material.dart';

class TripDetails extends StatelessWidget {
  const TripDetails({super.key});

  @override
  Widget build(BuildContext context) {

    return const Text('Let\'s seel some tripz');
  }
}