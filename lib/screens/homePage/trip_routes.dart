
import 'package:flutter/material.dart';

class TripRoutes extends StatelessWidget {
  const TripRoutes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Let\'s see some routez');
  }
}